import { createClient } from '@supabase/supabase-js';

const supabaseUrl = 'YOUR_URL';
const supabaseKey = 'YOUR_KEY';

// I need to read the url and key from supabaseClient.js or I can just run it using node where I import mockBackend.
